-- CREATE TABLE--
USE [DemoPaging]
CREATE TABLE [dbo].[DummyTBL](
	[id] [int] NOT NULL,
	[name] [varchar](150) NOT NULL,
 CONSTRAINT [PK_DummyTBL] PRIMARY KEY CLUSTERED 
(
	[id] ASC
)WITH (PAD_INDEX  = OFF, STATISTICS_NORECOMPUTE  = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS  = ON, ALLOW_PAGE_LOCKS  = ON) ON [PRIMARY]
) ON [PRIMARY]

GO

SET ANSI_PADDING OFF
GO

-- INSERT 2000 records
DECLARE @ID INT = 1;
WHILE(@ID <= 2000)
BEGIN
	INSERT INTO DummyTBL(id,name) VALUES (@ID,'Mr ' + CAST(@ID as VARCHAR(10)));
	SET @ID = @ID + 1;
END

SELECT * FROM DummyTBL

-- remove id = 9
delete DummyTBL WHERE id = 9


---- PAGING QUERY
-- pagesize = 10
-- pageindex = 2
-- start = pagesize * (pageindex - 1) + 1
-- end = start + pagesize-1

select rn,id,name from (
    select ROW_NUMBER() over (order by id asc) as rn, id,name
    from DummyTBL
) as x
where rn between ? and ?

select rn, 